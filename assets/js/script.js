// global variable

var startBtn = document.querySelector("#start-button");
var guessText = document.querySelector("#guess-text");
var gameEl = document.querySelector(".main-game");
var timer = document.querySelector("#timer-remaining");

// event listener to trigger start function
startBtn.addEventListener("click", function () {});

// when clicked, start the timer and initialize the game variables (wins, losses, current word)

// var of the word to be guessed

// display the blanks corresponding to the letters

// listen for user input through keyboard events

// validate the guess: check if it's valid letetr and not already guessed

// update the blanks with the correctly guessed letetrs

// check if the user has successfully guessed all the letters in the word

// if the word is fully guessed, display the score as one, show the winning message

// if the user runs out of time or attempts, display the score as zero, update the score display, show the loosing message

// start the timer when the game starts and decrement the time every second

// check if the time runs out before the word is fully guessed

// update the wins and losses count on the screen whenever the user wins or loses
